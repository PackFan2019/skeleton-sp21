package deque;

public class ArrayTest {

    public  static int[] insert(int[] arr, int item,int position){
        int[] res=new int[arr.length+1];
        if (position>=arr.length){
            position=arr.length;
        }

//        System.arraycopy(arr,0,res,0,position-1);
//        System.arraycopy(arr,position,res,,);


    }
}
